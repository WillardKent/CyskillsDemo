export type LearningPathData = {
    title: string;
    platform: string;
    duration: string;
    purpose: string;
    level: string;
    matchScoreGain: string;
    tagLabel: string;
    tagVariant: "danger" | "success" | "info" | "default";
    skills: string[];
};

